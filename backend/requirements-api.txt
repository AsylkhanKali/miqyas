# Minimal requirements for the Railway web server.
# Heavy ML / IFC packages (ifcopenshell, torch, scipy, trimesh) belong
# in the Celery worker image — the API only dispatches tasks, never runs them.

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1

# Celery (task dispatch only — workers run separately)
celery[redis]==5.4.0
redis==5.2.1

# Validation / Config
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# File handling
aiofiles==24.1.0
python-magic==0.4.27

# Schedule parsing (XER) — lxml is small (~5 MB)
lxml==5.3.0

# Report generation — needed for PDF endpoints
numpy==2.2.1
Pillow==11.1.0
reportlab==4.2.5

# Cloud Storage
boto3>=1.34.0

# Production Server
gunicorn==23.0.0

# Monitoring
sentry-sdk[fastapi]==2.19.2
structlog>=24.1.0
prometheus-fastapi-instrumentator>=7.0.0
